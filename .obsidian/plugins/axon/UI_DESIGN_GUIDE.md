# Axon Plugin - UI/UX 设计规范
## 基于 GitHub Primer Design System

---

## 🎨 设计哲学

Axon 插件严格遵循 **GitHub Primer Design System** 的设计语言，创造一个熟悉、直观的用户界面，就像使用 GitHub Issue 讨论区一样。

### 核心原则
- **一致性** - 与 GitHub 生态系统保持视觉一致
- **清晰性** - 清晰的信息层级和视觉层次
- **可访问性** - 支持键盘导航和屏幕阅读器
- **响应式** - 适配各种屏幕尺寸
- **主题支持** - 完美支持深色和浅色模式

---

## 🎯 整体布局

### 侧边栏布局
```
┌─────────────────────────────────┐
│  Axon Console    [清除]          │ ← Header (GitHub Issue Header)
├─────────────────────────────────┤
│                                 │
│  [用户消息卡片]                   │ ← Message Thread
│  [Axon回复卡片]                   │
│  [系统消息卡片]                   │
│                                 │
└─────────────────────────────────┘
┌─────────────────────────────────┐
│  ┌─────────────────────────────┐  │ ← Input Panel (GitHub Editor)
│  │ 输入您的消息...               │  │
│  │                             │  │
│  └─────────────────────────────┘  │
│                    [发送]        │ ← GitHub Primary Button
└─────────────────────────────────┘
```

### 设计特点
- **像 GitHub Issue 讨论区** - 消息显示为卡片，类似 GitHub 评论
- **Header 区域** - 带有标题和操作按钮，类似 Issue 头部
- **编辑器区域** - 类似 GitHub 的评论编辑器，带有 Write/Preview 暗示

---

## 🎨 颜色系统

### 浅色模式 (Light Mode)

#### 核心色彩
- **文本颜色**
  - 默认文本: `#24292f` (GitHub Dark Gray 900)
  - 次要文本: `#57606a` (GitHub Gray 600)
  - 辅助文本: `#656d76` (GitHub Gray 700)

- **背景颜色**
  - 主背景: `#ffffff` (GitHub White)
  - 次要背景: `#f6f8fa` (GitHub Gray 50)
  - 输入背景: `#ffffff` (GitHub White)
  - 悬停背景: `#f6f8fa` (GitHub Gray 50)

- **边框颜色**
  - 默认边框: `#d0d7de` (GitHub Gray 300)
  - 次要边框: `#d8dee4` (GitHub Gray 200)

#### 功能色彩
- **主色调 (Accent)**
  - 强调色: `#1f6feb` (GitHub Blue 600)
  - 文本色: `#0969da` (GitHub Blue 500)
  - 背景色: `#dbe9ff` (GitHub Blue 100)

- **成功色 (Success)**
  - 强调色: `#1f883d` (GitHub Green 700)
  - 文本色: `#1a7f37` (GitHub Green 600)
  - 背景色: `#dafbe1` (GitHub Green 100)

- **警告色 (Attention)**
  - 强调色: `#bf8700` (GitHub Yellow 700)
  - 文本色: `#9a6700` (GitHub Yellow 600)
  - 背景色: `#fff8c5` (GitHub Yellow 100)

- **错误色 (Danger)**
  - 强调色: `#cf222e` (GitHub Red 700)
  - 文本色: `#cf222e` (GitHub Red 700)
  - 背景色: `#ffebe9` (GitHub Red 100)

### 深色模式 (Dark Mode)

#### 核心色彩
- **文本颜色**
  - 默认文本: `#e6edf3` (GitHub Dark White)
  - 次要文本: `#8b949e` (GitHub Dark Gray 400)
  - 辅助文本: `#8b949e` (GitHub Dark Gray 400)

- **背景颜色**
  - 主背景: `#0d1117` (GitHub Dark 900)
  - 次要背景: `#161b22` (GitHub Dark 800)
  - 输入背景: `#0d1117` (GitHub Dark 900)
  - 悬停背景: `#161b22` (GitHub Dark 800)

- **边框颜色**
  - 默认边框: `#30363d` (GitHub Dark 400)
  - 次要边框: `#21262d` (GitHub Dark 500)

#### 功能色彩
- **主色调 (Accent)**
  - 强调色: `#1f6feb` (GitHub Blue 600)
  - 文本色: `#2f81f7` (GitHub Blue 400)
  - 背景色: `#1c2128` (GitHub Dark 700)

- **成功色 (Success)**
  - 强调色: `#238636` (GitHub Green 600)
  - 文本色: `#3fb950` (GitHub Green 400)
  - 背景色: `#1c2128` (GitHub Dark 700)

- **警告色 (Attention)**
  - 强调色: `#9e6a03` (GitHub Yellow 600)
  - 文本色: `#d29922` (GitHub Yellow 500)
  - 背景色: `#1c2128` (GitHub Dark 700)

- **错误色 (Danger)**
  - 强调色: `#da3633` (GitHub Red 600)
  - 文本色: `#f85149` (GitHub Red 400)
  - 背景色: `#1c2128` (GitHub Dark 700)

---

## 📝 消息卡片设计

### 消息卡片样式
```
┌─────────────────────────────────┐ ← 1px border (var(--color-border-default))
│                                 │
│  用户    14:32:15               │ ← Header (axon-message-header)
│                                 │
│  这是消息内容...                 │ ← Content (axon-message-content)
│                                 │
└─────────────────────────────────┘
```

### 设计细节
- **边框**: 1px 实线边框，使用 `var(--color-border-default)`
- **圆角**: `var(--border-radius)` = 6px
- **内边距**: `var(--spacing-3)` = 12px
- **阴影**: `var(--color-shadow-small)` - 微妙投影
- **左边框**: 3px 彩色左边框表示消息类型

### 消息类型颜色编码

#### 用户消息
- 左边框: `#1f6feb` (GitHub Blue)
- 背景色: `var(--color-accent-subtle)` = `#eaeef2`
- 类名: `axon-message-user`

#### Axon 消息
- 左边框: `#1f883d` (GitHub Green)
- 背景色: `var(--color-success-muted)` = `#dafbe1`
- 类名: `axon-message-assistant`

#### 系统消息
- 左边框: `#bf8700` (GitHub Yellow)
- 背景色: `var(--color-attention-muted)` = `#fff8c5`
- 类名: `axon-message-system`

#### 错误消息
- 左边框: `#cf222e` (GitHub Red)
- 背景色: `var(--color-danger-muted)` = `#ffebe9`
- 类名: `axon-message-error`

### Header 设计
- **类型标签**: 12px, 半粗体, 大写字母, 字母间距 0.5px
- **时间戳**: 12px, 次要文本颜色
- **布局**: 水平排列，左对齐

### Content 设计
- **字体大小**: 14px
- **行高**: 1.5
- **字体家族**: `var(--font-stack-sans)` = GitHub 字体栈
- **代码样式**: 85% 大小, 等宽字体, 带边框和背景

---

## ⌨️ 输入面板设计

### GitHub 评论编辑器风格
```
┌─────────────────────────────────┐ ← 1px border + 8px radius
│ ┌─────────────────────────────┐ │ ← Top area
│ │ 输入您的消息...               │ │ ← Textarea (100px min-height)
│ │                             │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │ ← Bottom area
│ │                    [发送]    │ │ ← Button container
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

### 设计细节

#### 文本框 (Textarea)
- **最小高度**: 100px
- **最大高度**: 自适应内容，最大 120px
- **边框**: 底部 1px 边框，`var(--color-border-default)`
- **聚焦状态**: 底部边框变蓝色 + 蓝色阴影 `0 0 0 3px var(--color-accent-muted)`
- **内边距**: `var(--spacing-3)` = 12px
- **字体**: GitHub 字体栈，14px
- **占位符**: 使用 `var(--color-fg-muted)` 颜色

#### Write/Preview 暗示
- 在右上角显示 "Write" 标签
- 12px 字体，半粗体
- 带边框和背景的标签样式

#### 按钮容器
- **背景色**: `var(--color-canvas-subtle)` = `#f6f8fa`
- **内边距**: `var(--spacing-3)` = 12px
- **布局**: 右对齐，水平排列

---

## 🔘 按钮设计

### 主要按钮 (Send Button)

#### 样式属性
- **颜色**: GitHub 绿色 `var(--color-success-emphasis)` = `#1f883d`
- **文字颜色**: `var(--color-fg-on-emphasis)` = `#ffffff`
- **边框**: 1px 绿色边框
- **圆角**: `var(--border-radius)` = 6px
- **字体**: GitHub 字体栈，14px，半粗体
- **内边距**: 8px 16px

#### 状态变化

##### 默认状态
```css
background-color: var(--color-success-emphasis);
border-color: var(--color-success-emphasis);
```

##### 悬停状态
```css
background-color: var(--color-success-fg);  /* #1a7f37 */
border-color: var(--color-success-fg);
box-shadow: var(--color-shadow-medium);
transform: translateY(-1px);
```

##### 激活状态
```css
box-shadow: var(--color-shadow-small);
transform: translateY(1px);
```

##### 禁用状态
```css
background-color: var(--color-border-muted);
border-color: var(--color-border-muted);
color: var(--color-fg-muted);
opacity: 0.7;
cursor: not-allowed;
```

### 次要按钮 (Clear Button)

#### 样式属性
- **颜色**: 透明背景
- **文字颜色**: `var(--color-fg-muted)`
- **边框**: 1px 边框 `var(--color-border-default)`
- **字体**: 12px，正常字重
- **内边距**: 4px 8px

#### 悬停状态
```css
background-color: var(--color-canvas-inset);
border-color: var(--color-border-default);
color: var(--color-fg-default);
```

---

## 🔤 字体系统

### 字体栈

#### 无衬线字体 (Sans-serif)
```css
--font-stack-sans: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji";
```

#### 等宽字体 (Monospace)
```css
--font-stack-mono: "SFMono-Regular", Consolas, "Liberation Mono", Menlo, Courier, monospace;
```

### 字体大小
- **0**: 12px - 标签、时间戳、辅助信息
- **1**: 14px - 正文、按钮文字、输入框
- **2**: 16px - 大标题
- **3**: 20px - 主标题

### 字重
- **正常**: 400 - 正文内容
- **半粗体**: 600 - 标题、按钮、强调文字

---

## 📐 间距系统

### 间距变量
```css
--spacing-1: 4px;   /* 最小间距 */
--spacing-2: 8px;   /* 小间距 */
--spacing-3: 12px;  /* 中等间距 */
--spacing-4: 16px;  /* 大间距 */
--spacing-5: 20px;  /* 特大间距 */
--spacing-6: 24px;  /* 最大间距 */
```

### 使用场景
- **4px**: 标签内边距、小按钮内边距
- **8px**: 元素间最小间距
- **12px**: 卡片内边距、按钮垂直内边距
- **16px**: 主要间距、容器内边距
- **20px**: 大容器间距
- **24px**: 页面边距

---

## 📱 响应式设计

### 断点
```css
@media (max-width: 768px) {
  /* 移动设备样式 */
}
```

### 移动设备适配

#### 主要变化
- **容器内边距**: 16px → 12px
- **消息卡片内边距**: 12px → 8px
- **文本框最小高度**: 100px → 80px
- **发送按钮**: 变更为全宽度
- **字体大小**: 16px（防止 iOS 缩放）

---

## 🌙 深色模式适配

### 实现方式
使用 CSS 变量 + `body.theme-dark` 选择器

```css
:root {
  /* 浅色模式变量 */
}

body.theme-dark {
  /* 深色模式变量覆盖 */
}
```

### 颜色映射

| 浅色模式 | 深色模式 | 说明 |
|---------|---------|------|
| `#ffffff` | `#0d1117` | 主背景 |
| `#f6f8fa` | `#161b22` | 次要背景 |
| `#24292f` | `#e6edf3` | 主文本 |
| `#57606a` | `#8b949e` | 次要文本 |
| `#d0d7de` | `#30363d` | 主边框 |

---

## ♿ 可访问性

### 焦点样式
```css
.axon-clear-button:focus-visible,
.axon-send-button:focus-visible {
  outline: 2px solid var(--color-accent-emphasis);
  outline-offset: 2px;
}
```

### 键盘导航
- **Tab**: 导航到下一个可聚焦元素
- **Enter**: 激活按钮
- **Ctrl+Enter** (或 **Cmd+Enter**): 发送消息

### 屏幕阅读器支持
- 语义化 HTML 元素
- 适当的 ARIA 标签（未来版本）
- 清晰的文本对比度

---

## ✨ 动画效果

### 消息滑入动画
```css
.axon-message {
  animation: slideIn 0.2s ease-out;
}

@keyframes slideIn {
  from {
    opacity: 0;
    transform: translateY(8px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

### 骨架屏加载动画
```css
.axon-message.loading::after {
  background: linear-gradient(
    90deg,
    transparent,
    var(--color-canvas-subtle),
    transparent
  );
  animation: shimmer 1.5s infinite;
}
```

---

## 📦 CSS 变量速查

### 颜色变量
```css
/* 文本颜色 */
--color-fg-default
--color-fg-muted
--color-fg-subtle
--color-fg-on-emphasis

/* 背景颜色 */
--color-canvas-default
--color-canvas-subtle
--color-canvas-inset

/* 边框颜色 */
--color-border-default
--color-border-muted

/* 功能颜色 */
--color-accent-fg
--color-accent-emphasis
--color-success-fg
--color-success-emphasis
--color-danger-fg
--color-danger-emphasis
--color-attention-fg
--color-attention-emphasis
```

### 字体变量
```css
--font-stack-sans
--font-stack-mono
--font-size-0  /* 12px */
--font-size-1  /* 14px */
--font-size-2  /* 16px */
--font-size-3  /* 20px */
--font-weight-normal
--font-weight-semibold
```

### 间距变量
```css
--spacing-1  /* 4px */
--spacing-2  /* 8px */
--spacing-3  /* 12px */
--spacing-4  /* 16px */
--spacing-5  /* 20px */
--spacing-6  /* 24px */
```

---

## 🎯 实现检查清单

### ✅ 已实现的 GitHub 设计规范

- [x] **颜色系统** - 使用 GitHub Primer 颜色变量
- [x] **深色模式** - 完整的深色模式支持
- [x] **消息卡片** - GitHub Issue 评论风格
- [x] **输入面板** - GitHub 评论编辑器风格
- [x] **按钮样式** - GitHub 按钮设计
- [x] **字体系统** - GitHub 字体栈
- [x] **间距系统** - 一致的间距变量
- [x] **圆角** - 6px 和 8px 圆角
- [x] **阴影** - GitHub 风格的微妙阴影
- [x] **边框** - 1px 细边框
- [x] **悬停效果** - GitHub 风格的悬停状态
- [x] **聚焦样式** - 蓝色聚焦轮廓
- [x] **动画** - 消息滑入动画
- [x] **响应式** - 移动设备适配
- [x] **滚动条** - GitHub 风格的滚动条
- [x] **代码块** - GitHub 风格的代码显示
- [x] **可访问性** - 键盘导航和焦点样式

---

## 📖 参考资源

- [GitHub Primer Design System](https://primer.style/)
- [GitHub Design Guidelines](https://github.com/github/design)
- [Primer Color System](https://primer.style/foundations/color)
- [Primer Typography](https://primer.style/foundations/typography)
- [Primer Spacing](https://primer.style/foundations/spacing)

---

**版本**: 1.0.0
**最后更新**: 2025-12-17
**设计规范版本**: GitHub Primer v15.0+
